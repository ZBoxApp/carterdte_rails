<div class="col-lg-12 text-center m-t-md">
    <h2>
        <%= t "messages.empty" %> <%= controller_name %>
    </h2>

    <p style="margin-top: 20px">
        <%= link_to "#{t("actions.add").titleize} #{controller_name.singularize}", 
            send("new_#{params[:controller].singularize}_path"),
            class: "btn btn-primary btn-lg" %>
    </p>
</div>